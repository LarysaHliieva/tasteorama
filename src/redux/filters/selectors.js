export const selectFiltersCategories = (state) => state.filters.selected.categories;

export const selectFiltersIngredients = (state) => state.filters.selected.ingredients;

export const selectSearchQuery = (state) => state.filters.selected.searchQuery;

export const selectCategoriesOptions = (state) => state.filters.options.categories;

export const selectIngredientsOptions = (state) => state.filters.options.ingredients;